# Homework 1 (02/09/2026)
In order to get the result of `homework.pdf`, you can run `homework1.py` with matplotlib installed on your system (or you can comment every line about matplotlib and use the graph generated within the `homework.typ` file).

The code is slightly complexified with the use `multiprocessing` library, but I thought it would be usefull because this program was at first taking a lot of time thanks to an error in the file parsing.